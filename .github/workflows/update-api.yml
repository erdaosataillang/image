name: Generate Image API JSON

on:
  push:
    paths:
      - 'images/**'
  workflow_dispatch: # 手動でActionを実行できるように追加

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate JSON
        run: |
          mkdir -p api
          # ここを独自ドメインのURLに書き換えました！
          echo '{"baseUrl": "https://file.sorairosystem.com/images/", "images": [' > api/images.json
          ls -1 images | grep -E '\.(jpg|jpeg|png|HEIC|heic|JPG|PNG)$' | sed 's/.*/"&",/' | sed '$ s/,$//' >> api/images.json
          echo ']}' >> api/images.json

      - name: Commit and push changes
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add api/images.json
          git commit -m "Auto-update images.json with custom domain" || echo "No changes to commit"
          git push
